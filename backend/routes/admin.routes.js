import express from "express";

const router = express.Router();

// TODO: Add admin routes
// - GET /campaigns (approve/reject campaigns)
// - POST /campaigns/:id/approve
// - POST /campaigns/:id/reject

export default router;
