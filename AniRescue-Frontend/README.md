# AniRescue Frontend

Production-ready React/Vite frontend for the AniRescue MERN crowdfunding platform.

## Setup

```bash
npm install
npm run dev
```

Create `.env` from `.env.example` and point `VITE_API_URL` to the existing backend API.
